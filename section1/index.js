// know the following commands
tsc --init // generates a tsconfig.json file

// understanding the tsconfig.json file
// mainly rootDir, outDir, etc.

tsc // after configuring the tsconfig.json file, tsc should compile the .ts files to their .js versions to the ourDir specified.